# EAS Build: skip postinstall scripts (turbo binary download, llama.rn, etc.)
# postinstall scripts are not needed on the EAS Linux build server.
# Expo native module setup happens via expo prebuild, not postinstall.
ignore-scripts true
